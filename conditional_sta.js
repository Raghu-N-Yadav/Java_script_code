// using conditional stements like if ..
// () ? : ; -- ternary operation like if else 
let workingLevel = 'hard';
 if (workingLevel === 'hard'){
     console.log('You will get a job.');
 }else {
     console.log('Cahnce are less Dear !!');
 }

 //reassinging the value of variable 
 workingLevel = 'medium';

 //using ternary operation
 (workingLevel === 'hard') ? console.log('You will definatly get a job.') : console.log('Cahnces are less.. try harder');

 